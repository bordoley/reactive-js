import type * as Iterable from "../../Iterable.js";
import ReadonlyArray_fromFactory from "../../ReadonlyArray/__internal__/ReadonlyArray.fromFactory.js";

const Iterable_fromFactory: Iterable.Signature["fromFactory"] =
  ReadonlyArray_fromFactory;

export default Iterable_fromFactory;
