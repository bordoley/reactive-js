{
  "name": "@reactive-js/perf",
  "version": "0.0.37",
  "private": true,
  "dependencies": {
    "@reactive-js/core": "^0.0.37",
    "benchmark": "^2.1.4",
    "callbag-basics": "^3.2.0",
    "rxjs": "^6.5.3"
  },
  "scripts": {
    "build": "yarn build-cjs && yarn build-mjs",
    "build-cjs": "tsc -p tsconfig.cjs.json",
    "build-mjs": "rm -rf ./dist/mjs && cp -r ./dist/cjs ./dist/mjs",
    "clean": "rm -rf dist"
  }
}
