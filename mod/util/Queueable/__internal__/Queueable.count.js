/// <reference types="./Queueable.count.d.ts" />

import { QueueableLike_count } from "../../../util.js";
const Queueable_count = (queue) => queue[QueueableLike_count];
export default Queueable_count;
