import { SideEffect1 } from "../../../functions.js";
import { QueueableLike } from "../../../util.js";
declare const Queueable_pushTo: <T>(queue: QueueableLike<T>) => SideEffect1<T>;
export default Queueable_pushTo;
