/// <reference types="./effects.d.ts" />
import { Continuation__yield, Continuation__now } from './__internal__/Continuation.create.mjs';

const __yield = Continuation__yield;
const __now = Continuation__now;

export { __now, __yield };
