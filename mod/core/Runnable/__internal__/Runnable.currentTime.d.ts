import { RunnableLike } from "../../../core.js";
declare const Runnable_currentTime: (options?: {
    readonly delay?: number;
    readonly delayStart?: boolean;
}) => RunnableLike<number>;
export default Runnable_currentTime;
