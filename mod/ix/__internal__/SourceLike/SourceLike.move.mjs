/// <reference types="./SourceLike.move.d.ts" />
import { SourceLike_move } from '../../../ix.mjs';

const SourceLike__move = (source) => {
    source[SourceLike_move]();
    return source;
};

export { SourceLike__move as default };
