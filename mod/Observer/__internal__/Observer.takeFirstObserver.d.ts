import { ObserverLike } from "../../types.js";
declare const Observer_createTakeFirstObserver: <T>(delegate: ObserverLike<T>, count: number) => ObserverLike<T>;
export default Observer_createTakeFirstObserver;
